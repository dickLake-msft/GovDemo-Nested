{
    "$schema": "https://schema.management.azure.com/schemas/2018-05-01/subscriptionDeploymentTemplate.json#",
    "contentVersion": "1.0.0.0",
    "parameters": {
        "rg-location": {
            "type": "string",
            "defaultValue": "usgovarizona",
            "allowedValues":["usgovvirginia","usgoviowa","usdodeast","usdodcentral","usgovtexas", "usgovarizona"],
            "metadata": {
                "description": "Location where resources will be held"
            }
        },
        "rg-name": {
            "type": "string",
            "defaultValue": "ContosoGov"
        }
    },
    "functions": [],
    "variables": {
        "WindowsServerRG": "WindowsServers-RG",
        "LinuxServerRG": "LinuxServers-RG",
        "StorageRG": "Storage-RG",
        "ContainersRG":"Containers-RG",
        "SOC": "SOC-RG",
        "Hub":"Hub-RG",
        "Vaults": "Vaults-RG",
        "DB": "Databases-RG",
        "WebApp":"WebApps-RG"
    },
    "resources": [{
        "name": "[concat(parameters('rg-name'), '-', if(equals(copyIndex(),0), variables('WindowsServerRG'), 
                                                         if(equals(copyIndex(), 1), variables('LinuxServerRG'), 
                                                         if(equals(copyIndex(),2), variables('StorageRG'), 
                                                         if(equals(copyIndex(),3), variables('ContainersRG'),
                                                         if(equals(copyIndex(),4),variables('SOC'),
                                                         if(equals(copyIndex(),5), variables('Hub'),
                                                         if(equals(copyIndex(),6), variables('Vaults'),
                                                         if(equals(copyIndex(),7), variables('DB'), variables('WebApp'))))))))))]",
        "type": "Microsoft.Resources/resourceGroups",
        "apiVersion": "2021-01-01",
        "location": "[parameters('rg-location')]",
        "dependsOn": [],
        "tags": {
                "createdBy": "dilake",
                "Purpose": "ContosoGov Demo"
            },
        "copy":{
            "name":"rg-copy",
            "count": 9
        }
    }
    ],
    "outputs": {}
}